//primer archivo de JS :D

//en esta clase estan explicando tipos de datos C:

//pero ya me los sabía B)

//placeholders values null, undefined

//object type value {name: "carlos"} como en los json

//SCOPE - - - - 

//es como el alcance de una variable global o local

var ji = "hola"; //variable con scope global

function fun() {
    var ja = "adios"; // variable local.
}

// Cosas raras de JS
// valores que tienen un boolean por default xd
// valores false: 0, "", null, undefined, NaN
// valores true: 1 en adelante, "con algo por dentro", cualquier function, cualquier object, cualquier array.


//operadores.

// clasicos: + - * /  : operadores binarios

// !=no, == = comparación (no compara tipo y valor), === = estrictamente igual (compara tipo y valor)

// < menor que > mayor que

// && el y

// || el o

// ++ sumar en 1

// += suma e iguala xd

// mismos que en java excepto por el triple igual xd wtf

